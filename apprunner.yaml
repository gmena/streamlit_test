version: 1.0
runtime: python3
build:
  commands:
    build:
      - pip install pipenv
      - pipenv install
run:
  runtime-version: 3.7
  command: pipenv run streamlit hello
  network:
    port: 8501
